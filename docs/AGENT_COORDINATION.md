# Agent coordination checklist

Coordination pull requests should follow these conventions:

- Use a PR title that starts with `[coord]`.
- Include a `Closes owner/repo#...` line in the PR body.
- Include a `Heartbeat (UTC)` line in the PR body.
